description = 'POS Clothes - Core'

repositories {
    mavenCentral()
}

dependencies {
    implementation "com.zaxxer:HikariCP:${hikariCPVersion}"
    implementation "com.mysql:mysql-connector-j:${jdbcMysqlVersion}"
    implementation "at.favre.lib:bcrypt:${bcryptVersion}"
}